import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app.ionic.loterias-caixa',
  webDir: 'www'
};

export default config;
